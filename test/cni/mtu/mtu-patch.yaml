apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-nginx-deployment
spec:
  replicas: 1
  template:
    spec:
      initContainers:
        - name: sfunnel-init
          image: sfunnel
          env:
            - name: DEBUG
              value: "0"
        - name: sfunnel-init-egress
          image: sfunnel
          env:
            - name: DEBUG
              value: "0"
